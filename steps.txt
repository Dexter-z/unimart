nx g @nx/express:app seller-service --directory=apps/seller-service --e2eTestRunner=none

nx g @nx/next:app apps/admin-ui

TO COUNT NUMBER OF LINES OF CODE 
find . -type f \( -name "*.ts" -o -name "*.tsx" \) ! -path "./node_modules/*" | xargs wc -l


To generate prisma
npx prisma db push

TO RUN THE CONSUMER
node apps/kafka-service/dist/main.js

TO RUN KAFKA LOCALLY
OPEN TWO COMMAND PROMPT TERMINALS
cd .. & cd .. & cd .. & cd .. & cd .. & cd kafka & cd kafka_2.13-3.9.1 & .\bin\windows\zookeeper-server-start.bat .\config\zookeeper.properties

cd .. & cd .. & cd .. & cd .. & cd .. & cd kafka & cd kafka_2.13-3.9.1 & .\bin\windows\kafka-server-start.bat .\config\server.properties

DYNAMIC FEATURES COMPLETED:
- OrdersTab in user-ui now fetches real orders from /order/api/get-user-orders
- Real-time order statistics (total, processing, shipped, delivered)
- Search functionality for orders
- Loading states and error handling with retry

To run stripe webhook service
stripe login

=== OPTION 1: Manual commands (run each in separate terminal) ===
stripe listen --forward-to localhost:6004/api/create-order
npx nx serve api-gateway
npx nx serve admin-service
npx nx serve auth-service
npx nx serve order-service
npx nx serve product-service
npm run admin-ui
npm run user-ui
npm run seller-ui

=== OPTION 2: VS Code Tasks (Recommended for development) ===
1. Open Command Palette (Ctrl+Shift+P)
2. Type "Tasks: Run Task"
3. Select "Start All Services" to run all services in separate terminals
   OR select individual services like "Start API Gateway", "Start Auth Service", etc.

=== OPTION 3: Automated scripts ===
# PowerShell (Windows):
./start-all-services.ps1

# Command Prompt (Windows):
start-all-services.bat

# Git Bash (Windows):
./start-all-services.sh